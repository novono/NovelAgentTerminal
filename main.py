from agents.manager_agent import ManagerAgent

if __name__ == "__main__":
    # Initialize the Manager Agent / 初始化主编 Agent
    agent = ManagerAgent()
    # Start the main loop / 启动主循环
    agent.run()
